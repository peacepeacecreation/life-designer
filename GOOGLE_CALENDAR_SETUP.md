# Налаштування Google Calendar API

Ця інструкція допоможе вам налаштувати інтеграцію з Google Calendar для вашого додатку Life Designer.

## Що вже зроблено

✅ Встановлено необхідні пакети (next-auth, googleapis)
✅ Створено API роути для авторизації та роботи з календарем
✅ Додано OAuth credentials в .env.local
✅ Оновлено компонент календаря для синхронізації з Google Calendar

## Важливо: Налаштування в Google Cloud Console

Для коректної роботи потрібно додати **Authorized redirect URI** в Google Cloud Console:

### Крок 1: Відкрийте Google Cloud Console

1. Перейдіть на https://console.cloud.google.com/
2. Виберіть ваш проект

### Крок 2: Налаштуйте OAuth 2.0 Client

1. Перейдіть до **APIs & Services** → **Credentials**
2. Знайдіть ваш OAuth 2.0 Client ID
3. Натисніть на нього для редагування

### Крок 3: Додайте Authorized redirect URIs

Додайте наступні URI до списку **Authorized redirect URIs**:

```
http://localhost:3077/api/auth/callback/google
```

Для production (коли буде деплой):
```
https://your-domain.com/api/auth/callback/google
```

### Крок 4: Увімкніть Google Calendar API

1. Перейдіть до **APIs & Services** → **Library**
2. Знайдіть "Google Calendar API"
3. Натисніть **Enable**

## Як користуватись

1. Запустіть dev server:
   ```bash
   pnpm dev
   ```

2. Відкрийте http://localhost:3077/calendar

3. Натисніть кнопку "Увійти через Google"

4. Після авторизації ви побачите всі події з вашого Google Calendar

5. Для створення події:
   - Клікніть на порожній слот в календарі
   - Введіть назву події
   - Подія автоматично синхронізується з Google Calendar

6. Для перегляду події:
   - Клікніть на існуючу подію
   - Можна відкрити її в Google Calendar

## Функціонал

✅ Перегляд подій з Google Calendar
✅ Створення нових подій
✅ Автоматична синхронізація
✅ Відкриття події в Google Calendar
✅ Локалізація українською
✅ Різні види календаря (місяць, тиждень, день, порядок денний)

## Майбутні можливості

- 🔄 Редагування існуючих подій
- 🗑️ Видалення подій
- 🔔 Налаштування нагадувань
- 📊 Інтеграція з Clockify для трекінгу часу
- 🎥 Додавання Zoom посилань до подій

## Безпека

⚠️ **ВАЖЛИВО**: Файл `.env.local` містить секретні ключі і **НЕ ПОВИНЕН** бути закоммічений в git.

Переконайтесь що `.env.local` додано в `.gitignore` (це вже зроблено).

## Troubleshooting

### Помилка "Unauthorized"
- Перевірте що ви увійшли через Google
- Переконайтеся що додано правильний redirect URI в Google Cloud Console

### Події не завантажуються
- Перевірте що Google Calendar API увімкнено в Google Cloud Console
- Натисніть кнопку "Оновити події"
- Перевірте консоль браузера на наявність помилок

### Помилка при створенні події
- Переконайтеся що у вас є дозволи на редагування календаря
- Перевірте що час події коректний

## Додаткова інформація

Документація Next-Auth: https://next-auth.js.org/
Google Calendar API: https://developers.google.com/calendar/api
